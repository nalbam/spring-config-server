spring:
  application:
    name: config
  profiles:
    active: ${PROFILE:default}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nalbam/spring-config-repo
          username: ${USERNAME:username}
          password: ${PASSWORD:password}
