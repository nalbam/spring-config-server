spring:
  application:
    name: config

---

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yanolja/yanolja-config-repo
          username: ${USERNAME:username}
          password: ${PASSWORD:password}

management:
  context-path: /admin

encrypt:
  key: ${ENCRYPT_KEY:RandomString}
