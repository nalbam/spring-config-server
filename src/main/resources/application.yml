server:
  port: ${PORT:8888}

---

spring:
  profiles: config1
server:
  port: 8081

---

spring:
  profiles: config2
server:
  port: 8082

---

management:
  context-path: /admin

logging:
  path: /data/logs
  level:
    root: warn

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nalbam/nalbam-config-repo
          username: ${USERNAME:username}
          password: ${PASSWORD:password}
